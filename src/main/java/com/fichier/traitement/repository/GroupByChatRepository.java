package com.fichier.traitement.repository;

import com.fichier.traitement.entity.GroupByChat;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GroupByChatRepository extends JpaRepository<GroupByChat,Long> {
}
